package com.tsironneau.java17;

/**
 * Rank classification for Moblins: GRUNT (weakest), VETERAN (moderate), ELITE (strongest).
 * Replaces magic integer constants (1, 2, 3) with type-safe enum values.
 */
public enum MoblinRank {
    GRUNT, VETERAN, ELITE
}
